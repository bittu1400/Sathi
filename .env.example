# Copy to .env.local and fill in. Never commit real values (this repo is public).
# Server-only keys must never be imported from client code.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=            # server only: payment routes, seed script

NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_SOS_SMS_NUMBER=           # E.164 team number for the demo (ends up in the client bundle)
NEXT_PUBLIC_DEMO=1                    # demo login buttons, /demo, mock checkout
NEXT_PUBLIC_DEMO_GMAIL=               # team Gmail; demo accounts are its +trekker/+rescue/+agency aliases (SPEC 5.3)

# Seed script only (scripts/seed-users.ts), never in Vercel:
SEED_PASSWORD=                        # password for every demo account
SEED_EMERGENCY_PHONE=                 # E.164 team phone, Maya's emergency contact (optional)

ESEWA_PRODUCT_CODE=EPAYTEST
ESEWA_SECRET_KEY=                     # eSewa test key (team docs, SPEC 7.7); server only
ESEWA_FORM_URL=https://rc-epay.esewa.com.np/api/epay/main/v2/form
ESEWA_STATUS_URL=https://rc.esewa.com.np/api/epay/transaction/status/

ANTHROPIC_API_KEY=                    # P2 assistant only (paid); leave empty

# Route planner (/api/plan and /api/places). Free tiers, no card.
ORS_API_KEY=                          # openrouteservice.org/dev — server only; 2,000 routes/day + 1,000 geocodes/day
CARTO_BASEMAPS_API_KEY=               # CARTO basemap styles; reaches the browser by design, not a secret
